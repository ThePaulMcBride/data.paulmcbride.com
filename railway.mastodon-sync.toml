[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.sync"
watchPatterns = [
  "**",
  "!content/**",
]

[deploy]
startCommand = "sync-mastodon-and-push"
cronSchedule = "17 * * * *"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
